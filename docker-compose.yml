version: '3'

services:
  spring-app:
    build:
      context: .
      dockerfile: Dockerfile-Spring
    ports:
      - "8080:8080"
    depends_on:
      - redis
  redis:
    image: redis
    container_name: my-redis-container
    ports:
      - "6379:6379"

